- hosts: all
  collections:
    - arubanetworks.aoscx
  gather_facts: False
  tasks:
  - name: Execute show run on the switch
    aoscx_command:
      commands:
        - show run
      output_file_format: plain-text
