- hosts: all
  collections:
    - arubanetworks.aoscx
  gather_facts: False
  tasks:
  - name: Execute show run on the switch
    aoscx_command:
      commands:
        - show run
      register: running_output
      
  - name: Display running output
    debug:
      var: running_output
